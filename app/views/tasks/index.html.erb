<h1>My Tasks</h1>

<ul class="fa-ul" id="tasks">
  <% @tasks.each do |task| %>
    <li><span class="fa-li"><i class=<%= task.view_completion %>></i></span><%= link_to task.title, show_task_path(task) %></li>
  <% end %>
</ul>
